# =============================================================================
# Discord Bot — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# See docs: /docs/getting-started/environment-variables

# --- Database ----------------------------------------------------------------
# PostgreSQL connection URL. Default works with the docker-compose.yml stack.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/community-bot?schema=public"

# --- Redis -------------------------------------------------------------------
# Redis connection URL for EventBus pub/sub and BullMQ job queue.
REDIS_URL="redis://localhost:6379/0"

# --- Discord Application ----------------------------------------------------
# From https://discord.com/developers/applications → your app.
# Application ID is on the General Information tab.
DISCORD_APPLICATION_ID=""

# Public Key is on the General Information tab.
DISCORD_APPLICATION_PUBLIC_KEY=""

# Bot Token is on the Bot tab. Click "Reset Token" to generate a new one.
# This is the same token used in the web dashboard's DISCORD_BOT_TOKEN.
DISCORD_APPLICATION_BOT_TOKEN=""

# --- Discord Bot Presence ----------------------------------------------------
# Customize the bot's status message and activity type.
DISCORD_DEFAULT_STATUS="over the Wolf Lair"
DISCORD_DEFAULT_ACTIVITY_TYPE="Custom"

# Only used when DISCORD_DEFAULT_ACTIVITY_TYPE is "Streaming".
DISCORD_DEFAULT_ACTIVITY_URL=""

# How often the bot rotates its activity (1-1440 minutes).
DISCORD_ACTIVITY_INTERVAL_MINUTES="15"

# --- Bot Owner & Guild -------------------------------------------------------
# To get these IDs, enable Developer Mode in Discord:
#   User Settings → Advanced → Developer Mode → ON
# Then right-click a user/server/channel → "Copy ID".

# Your personal Discord user ID (receives error DMs and has owner-only access).
OWNER_ID=""

# The primary server (guild) the bot operates in.
MAIN_GUILD_ID=""

# The primary channel for bot log messages.
MAIN_CHANNEL_ID=""

# --- Twitch API --------------------------------------------------------------
# Client ID from https://dev.twitch.tv/console/apps → your app.
# This is the same Twitch app used by the Twitch bot and web dashboard.
# Required for checking Twitch stream status for live notifications.
TWITCH_APPLICATION_CLIENT_ID=""

# --- Express Server ----------------------------------------------------------
HOST="localhost"
PORT="3141"
CORS_ORIGIN="*"

# --- Misc --------------------------------------------------------------------
VERSION="1.0.0"
NODE_ENV="development"
