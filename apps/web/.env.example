# =============================================================================
# Web Dashboard — Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values.
# See docs: /docs/getting-started/environment-variables

# --- Database ----------------------------------------------------------------
# PostgreSQL connection URL. Default works with the docker-compose.yml stack.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/community-bot?schema=public"

# --- Authentication (better-auth) --------------------------------------------
# Secret used to sign session tokens. Must be at least 32 characters.
# Generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=

# The public URL of the web dashboard (used for OAuth callback URLs).
BETTER_AUTH_URL=http://localhost:3001

# Allowed CORS origin (should match BETTER_AUTH_URL in most cases).
CORS_ORIGIN=http://localhost:3001

# --- Discord OAuth -----------------------------------------------------------
# From https://discord.com/developers/applications → your app → OAuth2 tab.
# This is the same Discord application used by the Discord bot.
DISCORD_APPLICATION_ID=
DISCORD_APPLICATION_CLIENT_SECRET=

# Bot token from the Discord Developer Portal → your app → Bot tab.
# Required for fetching guild channels and roles via the Discord REST API.
DISCORD_BOT_TOKEN=

# --- Twitch OAuth ------------------------------------------------------------
# From https://dev.twitch.tv/console/apps → your app.
# This is the same Twitch application used by the Twitch bot.
TWITCH_APPLICATION_CLIENT_ID=
TWITCH_APPLICATION_CLIENT_SECRET=

# --- Redis -------------------------------------------------------------------
# Redis connection URL for EventBus pub/sub. Default works with docker-compose.
REDIS_URL=redis://localhost:6379

# --- Branding (all optional) -------------------------------------------------
# These customize the landing page, footer, and cookie banner.

# Links shown in the cookie banner and footer.
NEXT_PUBLIC_PRIVACY_POLICY_URL=
NEXT_PUBLIC_TERMS_OF_SERVICE_URL=

# Channel link and name displayed on the landing page.
NEXT_PUBLIC_CHANNEL_URL=
NEXT_PUBLIC_CHANNEL_NAME=

# Logo images. LOGO_DARK_URL is used in dark mode (falls back to LOGO_URL).
NEXT_PUBLIC_LOGO_URL=
NEXT_PUBLIC_LOGO_DARK_URL=

# Copyright holder shown in the footer.
NEXT_PUBLIC_COPYRIGHT_NAME=
NEXT_PUBLIC_COPYRIGHT_URL=

# Comma-separated social URLs. Icons are auto-detected from hostname
# (twitter, github, twitch, youtube, etc.).
# Example: https://twitter.com/you,https://github.com/you
NEXT_PUBLIC_SOCIAL_LINKS=
